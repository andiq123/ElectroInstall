{
  "rules": {
    "crm_requests": {
      "$id": {
        ".write": "!data.exists() ? (newData.exists() && newData.child('id').isString() && newData.child('name').isString() && newData.child('phone').isString() && newData.child('message').isString() && (newData.child('locale').val() == 'ro' || newData.child('locale').val() == 'ru') && newData.child('emailSent').isBoolean() && newData.child('createdAt').isString() && newData.child('status').val() == 'new') : auth != null"
      }
      ,
      ".read": "auth != null"
    }
  }
}
